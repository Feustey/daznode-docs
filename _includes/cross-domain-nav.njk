<!-- Navigation Cross-Domain -->
<nav class="cross-domain-nav" aria-label="Navigation inter-domaines">
  <div class="nav-sections">
    <div class="nav-section commercial">
      <span class="nav-label">🏠 Site Principal</span>
      <div class="nav-links">
        <a href="https://dazno.de" class="nav-link" data-utm="home">Accueil</a>
        <a href="https://dazno.de/solutions" class="nav-link" data-utm="solutions">Solutions</a>
        <a href="https://dazno.de/pricing" class="nav-link" data-utm="pricing">Tarifs</a>
        <a href="https://dazno.de/demo" class="nav-link cta-primary" data-utm="demo">Démo Gratuite</a>
      </div>
    </div>
    <div class="nav-section documentation">
      <span class="nav-label">📚 Documentation</span>
      <div class="nav-links">
        <a href="/" class="nav-link current" data-utm="docs-home">Guides</a>
        <a href="/devs/api/" class="nav-link" data-utm="api-docs">API</a>
        <a href="/getting-started/" class="nav-link" data-utm="getting-started">Démarrer</a>
      </div>
    </div>
  </div>
</nav>

<style>
.cross-domain-nav {
  background: linear-gradient(135deg, #1e293b 0%, #334155 100%);
  border-bottom: 1px solid rgba(255, 255, 255, 0.1);
  padding: 0.75rem 0;
  margin-bottom: 0;
}

.nav-sections {
  max-width: 1200px;
  margin: 0 auto;
  display: flex;
  gap: 2rem;
  padding: 0 1rem;
  align-items: center;
}

.nav-section {
  display: flex;
  align-items: center;
  gap: 1rem;
}

.nav-label {
  font-size: 0.875rem;
  font-weight: 600;
  color: #94a3b8;
  white-space: nowrap;
}

.nav-links {
  display: flex;
  gap: 0.75rem;
  align-items: center;
}

.nav-link {
  color: #e2e8f0;
  text-decoration: none;
  font-size: 0.9rem;
  font-weight: 500;
  padding: 0.5rem 0.75rem;
  border-radius: 0.375rem;
  transition: all 0.2s ease;
  border: 1px solid transparent;
}

.nav-link:hover {
  background: rgba(255, 255, 255, 0.1);
  color: #ffffff;
  text-decoration: none;
}

.nav-link.current {
  background: rgba(59, 130, 246, 0.2);
  color: #60a5fa;
  border-color: rgba(59, 130, 246, 0.3);
}

.nav-link.cta-primary {
  background: linear-gradient(135deg, #3b82f6 0%, #1d4ed8 100%);
  color: #ffffff;
  font-weight: 600;
  box-shadow: 0 2px 4px rgba(59, 130, 246, 0.3);
}

.nav-link.cta-primary:hover {
  background: linear-gradient(135deg, #2563eb 0%, #1e40af 100%);
  box-shadow: 0 4px 8px rgba(59, 130, 246, 0.4);
  transform: translateY(-1px);
}

/* Responsive */
@media (max-width: 768px) {
  .nav-sections {
    flex-direction: column;
    gap: 1rem;
    align-items: flex-start;
  }
  
  .nav-section {
    flex-direction: column;
    align-items: flex-start;
    gap: 0.5rem;
    width: 100%;
  }
  
  .nav-links {
    flex-wrap: wrap;
    gap: 0.5rem;
  }
  
  .nav-link {
    font-size: 0.85rem;
    padding: 0.4rem 0.6rem;
  }
}

@media (max-width: 480px) {
  .cross-domain-nav {
    padding: 0.5rem 0;
  }
  
  .nav-links {
    width: 100%;
    justify-content: flex-start;
  }
  
  .nav-link {
    flex: 0 0 auto;
  }
}

/* DNS Prefetch pour optimisation des performances */
.nav-link[href^="https://dazno.de"] {
  /* Les liens externes auront un préfetch DNS automatique */
}
</style>

<script>
// Tracking cross-domain avec UTM et Analytics
document.addEventListener('DOMContentLoaded', function() {
  const crossDomainLinks = document.querySelectorAll('.cross-domain-nav .nav-link[href^="https://dazno.de"]');
  
  crossDomainLinks.forEach(link => {
    link.addEventListener('click', function(e) {
      const utmContent = this.getAttribute('data-utm') || 'cross-domain-nav';
      const currentPage = window.location.pathname;
      
      // Ajouter UTM parameters
      const url = new URL(this.href);
      url.searchParams.set('utm_source', 'docs');
      url.searchParams.set('utm_medium', 'internal_link');
      url.searchParams.set('utm_campaign', 'docs_to_commercial');
      url.searchParams.set('utm_content', utmContent);
      url.searchParams.set('utm_term', currentPage);
      
      this.href = url.toString();
      
      // Analytics event
      if (typeof gtag !== 'undefined') {
        gtag('event', 'cross_domain_click', {
          source_domain: 'docs.dazno.de',
          target_domain: 'dazno.de',
          link_context: utmContent,
          source_page: currentPage,
          user_journey_stage: 'documentation_to_commercial'
        });
      }
    });
  });
});
</script>