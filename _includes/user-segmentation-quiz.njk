<!-- Quiz de segmentation utilisateur -->
<div id="user-segmentation-quiz" class="quiz-overlay" style="display: none;">
  <div class="quiz-modal">
    <div class="quiz-header">
      <h3>🎯 Personnalisez votre expérience</h3>
      <p>2 questions rapides pour vous orienter vers le contenu le plus adapté</p>
    </div>
    
    <div class="quiz-content">
      <div class="quiz-step active" id="step-1">
        <h4>Quel est votre niveau avec Bitcoin ?</h4>
        <div class="quiz-options">
          <button class="quiz-option" data-step="1" data-answer="beginner">
            <span class="option-icon">🌱</span>
            <div>
              <strong>Débutant</strong>
              <small>J'apprends les bases de Bitcoin</small>
            </div>
          </button>
          <button class="quiz-option" data-step="1" data-answer="intermediate">
            <span class="option-icon">⚡</span>
            <div>
              <strong>Intermédiaire</strong>
              <small>J'ai des connaissances mais je veux approfondir</small>
            </div>
          </button>
          <button class="quiz-option" data-step="1" data-answer="expert">
            <span class="option-icon">🔧</span>
            <div>
              <strong>Expert</strong>
              <small>Je développe ou gère des solutions Bitcoin</small>
            </div>
          </button>
        </div>
      </div>
      
      <div class="quiz-step" id="step-2">
        <h4>Quel est votre objectif principal ?</h4>
        <div class="quiz-options">
          <button class="quiz-option" data-step="2" data-answer="learn">
            <span class="option-icon">📚</span>
            <div>
              <strong>Apprendre</strong>
              <small>Comprendre Bitcoin et ses technologies</small>
            </div>
          </button>
          <button class="quiz-option" data-step="2" data-answer="implement">
            <span class="option-icon">⚡</span>
            <div>
              <strong>Implémenter</strong>
              <small>Configurer mon nœud Lightning Network</small>
            </div>
          </button>
          <button class="quiz-option" data-step="2" data-answer="develop">
            <span class="option-icon">💻</span>
            <div>
              <strong>Développer</strong>
              <small>Créer des applications Bitcoin/Lightning</small>
            </div>
          </button>
          <button class="quiz-option" data-step="2" data-answer="business">
            <span class="option-icon">🏢</span>
            <div>
              <strong>Business</strong>
              <small>Intégrer Bitcoin dans mon activité</small>
            </div>
          </button>
        </div>
      </div>
      
      <div class="quiz-step" id="step-result">
        <div id="result-content">
          <!-- Le contenu sera injecté dynamiquement -->
        </div>
      </div>
    </div>
    
    <div class="quiz-footer">
      <button class="quiz-skip" onclick="closeUserQuiz()">Ignorer</button>
      <div class="quiz-progress">
        <div class="progress-bar">
          <div class="progress-fill" id="quiz-progress"></div>
        </div>
        <span id="step-counter">1/2</span>
      </div>
    </div>
  </div>
</div>

<style>
  .quiz-overlay {
    position: fixed;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background: rgba(0, 0, 0, 0.8);
    z-index: 10000;
    display: flex;
    align-items: center;
    justify-content: center;
    animation: fadeIn 0.3s ease-out;
  }
  
  .quiz-modal {
    background: var(--background, #fff);
    border-radius: 16px;
    padding: 2rem;
    max-width: 500px;
    width: 90%;
    max-height: 80vh;
    box-shadow: 0 20px 60px rgba(0, 0, 0, 0.3);
    position: relative;
    animation: slideUp 0.4s ease-out;
  }
  
  .quiz-header h3 {
    margin: 0 0 0.5rem 0;
    color: var(--primary, #2563eb);
    font-size: 1.4rem;
  }
  
  .quiz-header p {
    margin: 0 0 1.5rem 0;
    color: var(--foreground-muted, #666);
    font-size: 0.95rem;
  }
  
  .quiz-step {
    display: none;
    animation: fadeIn 0.3s ease-out;
  }
  
  .quiz-step.active {
    display: block;
  }
  
  .quiz-step h4 {
    margin: 0 0 1.5rem 0;
    font-size: 1.1rem;
    color: var(--foreground, #333);
  }
  
  .quiz-options {
    display: flex;
    flex-direction: column;
    gap: 0.75rem;
  }
  
  .quiz-option {
    display: flex;
    align-items: center;
    gap: 1rem;
    padding: 1rem;
    border: 2px solid var(--border, #e5e7eb);
    border-radius: 12px;
    background: var(--card, #fff);
    cursor: pointer;
    transition: all 0.2s ease;
    text-align: left;
    width: 100%;
  }
  
  .quiz-option:hover {
    border-color: var(--primary, #2563eb);
    transform: translateY(-2px);
    box-shadow: 0 4px 12px rgba(37, 99, 235, 0.1);
  }
  
  .option-icon {
    font-size: 1.5rem;
    min-width: 40px;
  }
  
  .quiz-option div {
    flex: 1;
  }
  
  .quiz-option strong {
    display: block;
    font-size: 1rem;
    margin-bottom: 0.25rem;
    color: var(--foreground, #333);
  }
  
  .quiz-option small {
    color: var(--foreground-muted, #666);
    font-size: 0.85rem;
    line-height: 1.3;
  }
  
  .quiz-footer {
    display: flex;
    justify-content: space-between;
    align-items: center;
    margin-top: 2rem;
    padding-top: 1rem;
    border-top: 1px solid var(--border, #e5e7eb);
  }
  
  .quiz-skip {
    background: none;
    border: none;
    color: var(--foreground-muted, #666);
    cursor: pointer;
    font-size: 0.9rem;
    text-decoration: underline;
  }
  
  .quiz-progress {
    display: flex;
    align-items: center;
    gap: 0.75rem;
  }
  
  .progress-bar {
    width: 80px;
    height: 4px;
    background: var(--border, #e5e7eb);
    border-radius: 2px;
    overflow: hidden;
  }
  
  .progress-fill {
    height: 100%;
    background: var(--primary, #2563eb);
    width: 50%;
    transition: width 0.3s ease;
  }
  
  #step-counter {
    font-size: 0.85rem;
    color: var(--foreground-muted, #666);
    font-weight: 500;
  }
  
  .result-card {
    background: linear-gradient(135deg, var(--primary, #2563eb), #1d4ed8);
    color: white;
    padding: 1.5rem;
    border-radius: 12px;
    margin-bottom: 1.5rem;
  }
  
  .result-card h4 {
    margin: 0 0 0.5rem 0;
    font-size: 1.2rem;
  }
  
  .result-card p {
    margin: 0 0 1rem 0;
    opacity: 0.9;
  }
  
  .result-actions {
    display: flex;
    flex-direction: column;
    gap: 0.75rem;
  }
  
  .result-btn {
    display: inline-flex;
    align-items: center;
    gap: 0.5rem;
    padding: 0.75rem 1rem;
    background: rgba(255, 255, 255, 0.15);
    color: white;
    text-decoration: none;
    border-radius: 8px;
    font-weight: 500;
    transition: all 0.2s ease;
    border: 1px solid rgba(255, 255, 255, 0.2);
  }
  
  .result-btn:hover {
    background: rgba(255, 255, 255, 0.25);
    transform: translateY(-1px);
  }
  
  .result-btn.primary {
    background: white;
    color: var(--primary, #2563eb);
  }
  
  .result-btn.primary:hover {
    background: #f8fafc;
    color: var(--primary, #2563eb);
  }
  
  @keyframes fadeIn {
    from { opacity: 0; }
    to { opacity: 1; }
  }
  
  @keyframes slideUp {
    from {
      opacity: 0;
      transform: translateY(30px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }
  
  @media (max-width: 480px) {
    .quiz-modal {
      padding: 1.5rem;
      width: 95%;
    }
    
    .quiz-option {
      padding: 0.75rem;
      gap: 0.75rem;
    }
    
    .result-actions {
      gap: 0.5rem;
    }
  }
  
  @media (prefers-color-scheme: dark) {
    .quiz-modal {
      background: #18181b;
      color: #f4f4f5;
    }
    
    .quiz-option {
      background: #27272a;
      border-color: #3f3f46;
    }
    
    .quiz-option:hover {
      border-color: #60a5fa;
    }
    
    .quiz-option strong {
      color: #f4f4f5;
    }
    
    .quiz-option small {
      color: #a1a1aa;
    }
  }
</style>

<script>
  let userAnswers = {};
  let currentStep = 1;

  function initUserQuiz() {
    // Afficher le quiz après 3 secondes si pas déjà fait
    if (!localStorage.getItem('userSegmentationDone')) {
      setTimeout(() => {
        document.getElementById('user-segmentation-quiz').style.display = 'flex';
        
        // Track quiz shown
        if (typeof umami !== 'undefined') {
          umami.track('Segmentation Quiz Shown');
        }
      }, 3000);
    }

    // Gérer les clics sur les options
    document.querySelectorAll('.quiz-option').forEach(button => {
      button.addEventListener('click', function() {
        const step = this.dataset.step;
        const answer = this.dataset.answer;
        
        userAnswers[`step${step}`] = answer;
        
        if (step === '1') {
          nextQuizStep();
        } else if (step === '2') {
          showQuizResult();
        }
      });
    });
  }

  function nextQuizStep() {
    document.getElementById('step-1').classList.remove('active');
    document.getElementById('step-2').classList.add('active');
    
    // Update progress
    document.getElementById('quiz-progress').style.width = '100%';
    document.getElementById('step-counter').textContent = '2/2';
    
    currentStep = 2;
  }

  function showQuizResult() {
    document.getElementById('step-2').classList.remove('active');
    document.getElementById('step-result').classList.add('active');
    
    // Déterminer le profil utilisateur
    const profile = determineUserProfile(userAnswers);
    
    // Générer le contenu de résultat
    const resultContent = generateResultContent(profile);
    document.getElementById('result-content').innerHTML = resultContent;
    
    // Stocker le profil utilisateur
    localStorage.setItem('userProfile', JSON.stringify(profile));
    localStorage.setItem('userSegmentationDone', 'true');
    
    // Track segmentation result
    if (typeof umami !== 'undefined') {
      umami.track('User Segmented', {
        level: userAnswers.step1,
        objective: userAnswers.step2,
        profile: profile.type
      });
    }
    
    // Masquer le footer pour le résultat
    document.querySelector('.quiz-footer').style.display = 'none';
  }

  function determineUserProfile(answers) {
    const level = answers.step1;
    const objective = answers.step2;
    
    // Logique de segmentation
    if (level === 'beginner' && (objective === 'learn' || objective === 'implement')) {
      return {
        type: 'beginner-learner',
        title: '🌱 Apprenant Bitcoin',
        description: 'Parfait pour débuter votre parcours Bitcoin avec des explications claires et progressives.',
        primaryAction: '/getting-started/',
        primaryLabel: 'Guide Bitcoin pour débutants (5 min)',
        secondaryAction: '/bitcoin/introduction/',
        secondaryLabel: 'Comprendre les fondamentaux'
      };
    }
    
    if ((level === 'intermediate' || level === 'expert') && objective === 'develop') {
      return {
        type: 'developer',
        title: '💻 Développeur Bitcoin',
        description: 'Ressources techniques avancées pour développer sur Bitcoin et Lightning Network.',
        primaryAction: '/devs/',
        primaryLabel: 'Documentation développeur',
        secondaryAction: '/devs/examples/',
        secondaryLabel: 'Exemples de code'
      };
    }
    
    if (objective === 'implement' && level !== 'beginner') {
      return {
        type: 'implementer',
        title: '⚡ Expert Lightning',
        description: 'Guides pratiques pour configurer et optimiser votre infrastructure Lightning Network.',
        primaryAction: '/lightning-network/',
        primaryLabel: 'Configurer mon nœud Lightning (15 min)',
        secondaryAction: '/solutions/dazbox/',
        secondaryLabel: 'Solution DazBox clé en main'
      };
    }
    
    if (objective === 'business') {
      return {
        type: 'business',
        title: '🏢 Professionnel Bitcoin',
        description: 'Solutions professionnelles pour intégrer Bitcoin dans votre activité commerciale.',
        primaryAction: '/token4good/',
        primaryLabel: 'Rejoindre 2500+ professionnels Bitcoin',
        secondaryAction: '/solutions/dazpay/',
        secondaryLabel: 'Solution de paiement DazPay'
      };
    }
    
    // Fallback
    return {
      type: 'explorer',
      title: '🎯 Explorateur Bitcoin',
      description: 'Découvrez toutes les facettes de Bitcoin et choisissez votre parcours.',
      primaryAction: '/getting-started/',
      primaryLabel: 'Commencer l\'exploration',
      secondaryAction: '/glossary/',
      secondaryLabel: 'Consulter le glossaire'
    };
  }

  function generateResultContent(profile) {
    return `
      <div class="result-card">
        <h4>${profile.title}</h4>
        <p>${profile.description}</p>
        <div class="result-actions">
          <a href="${profile.primaryAction}" class="result-btn primary" onclick="closeUserQuiz()">
            ${profile.primaryLabel} →
          </a>
          <a href="${profile.secondaryAction}" class="result-btn" onclick="closeUserQuiz()">
            ${profile.secondaryLabel}
          </a>
        </div>
      </div>
    `;
  }

  function closeUserQuiz() {
    document.getElementById('user-segmentation-quiz').style.display = 'none';
  }

  // Initialiser le quiz au chargement de la page
  document.addEventListener('DOMContentLoaded', initUserQuiz);
</script>